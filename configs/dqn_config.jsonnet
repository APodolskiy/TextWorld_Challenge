{
    training: {
        max_nb_steps_per_episode: 50,
        batch_size: 6,
        nb_epochs: 10,
    },
    model: {
        embedding_size: 100,
        rnn_hidden_size: 128,
        dropout_between_rnn_layers: 0.5
    },
}