{
    training: {
        max_nb_steps_per_episode: 200,
        batch_size: 5,
        nb_epochs: 10000,
    },
    model: {
        embedding_size: 100,
        rnn_hidden_size: 256,
        dropout_between_rnn_layers: 0.5
    },
}